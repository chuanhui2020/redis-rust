你是一个“主控智能体（Master Agent）”，负责监督一个“执行智能体（Subagent）”持续完成任务。

⚠️ 重要要求：
- 所有思考、输出、文档、代码注释必须使用中文
- 不要切换为英文
- 持续执行任务，不要等待人工确认或中断流程
- 不要询问用户“是否继续”，直接推进任务直到完成

---

# 🎯 目标（Goal）

使用 Rust 从零实现一个 Redis-like 的高性能缓存系统，并且：

1. 功能正确
2. 可运行
3. 有测试
4. 有文档
5. **必须通过严格的本地 benchmark，对标 Redis**

---

# 🚨 强制约束

- 必须使用 Rust（stable）
- 所有核心逻辑必须是 Rust
- 使用 `cargo build --release` 构建
- 使用 `cargo test` 验证
- 不允许用其他语言实现核心逻辑

---

# ⚙️ 必须实现的功能

## 基础能力
- TCP server
- 多客户端并发连接
- Redis-like 协议（优先 RESP）

## 命令支持
- PING
- SET key value
- GET key
- DEL key
- EXISTS key
- EXPIRE key seconds
- TTL key

## TTL
- 支持过期
- 过期 key 不可读取
- TTL 行为正确

## 持久化
至少实现一种：
- snapshot
或
- append-only log（AOF/WAL）

必须支持：
- 重启恢复
- 测试覆盖

---

# ⚡ 性能（硬性验收条件）

必须在本地进行严格 benchmark，对标 Redis：

## Benchmark规则
- 同一台机器
- 同一客户端工具（如 redis-benchmark）
- 相同参数（连接数、pipeline、数据大小等）
- 同样 workload

## 必须测试
- PING
- SET
- GET
- 并发场景

## 性能门槛（必须满足）
- PING ≥ Redis 的 90%
- SET ≥ Redis 的 85%
- GET ≥ Redis 的 85%
- 并发混合 ≥ Redis 的 80%

❗ 如果未达标 → 不允许结束任务，必须持续优化

---

# 🧪 正确性优先

- 不允许为了 benchmark 破坏正确性
- TTL 必须真实生效
- 不允许 mock benchmark
- 不允许硬编码结果
- 不允许针对 benchmark 特判逻辑

---

# 📊 进度汇报机制（强制）

你必须定期输出进度报告，格式如下：

## 📍 进度报告
- 当前阶段：
- 已完成：
- 正在做：
- 下一步：
- 是否阻塞：
- 是否满足最终验收条件（是/否）：

## 汇报频率（必须遵守）
- 每完成一个功能模块必须汇报
- 或每执行约 5~10 步必须汇报
- 不允许长时间无输出

⚠️ 汇报后必须继续执行任务，不要等待用户回复

---

# 🧠 执行策略

按阶段推进：

1. 项目结构设计
2. TCP server
3. 协议解析
4. 内存存储
5. PING / SET / GET
6. DEL / EXISTS
7. TTL
8. 持久化
9. 测试
10. benchmark
11. 性能优化（直到达标）

---

# ✅ 最终完成标准（全部满足才允许结束）

1. Rust 项目可 build（release）
2. 所有测试通过
3. 所有命令可用
4. TTL 正确
5. 持久化可恢复
6. benchmark 已执行（本地）
7. benchmark 达到性能门槛
8. README 完整（中文）
9. 有 benchmark 结果记录

---

# 🚫 严格禁止

- 提前结束任务
- 输出“基本完成”
- 跳过 benchmark
- 使用假数据
- 等待用户确认
- 输出英文
- 构建一个空壳项目

---

# 🎬 启动

现在开始执行：

先输出：
1. 项目架构设计（Rust）
2. 模块划分
3. 技术选型
4. benchmark 方案

然后立即进入实现阶段，并持续推进直到完成。